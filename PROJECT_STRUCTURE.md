# Project Structure

- `fish_detection_yolov5.ipynb` — cleaned inference/training workflow
- `weights/best.pt` — supplied trained YOLOv5 checkpoint
- `requirements.txt` — Python dependencies
- `README.md` — project documentation
- `.gitignore` — excludes local datasets, media and YOLO run artifacts

The original dataset is not included.
